三层架构设计
controller基于资源，使用restful思想设计
service和功能对应
mapper和表对应

对模块化的理解
1、将每一个模块看成一个jar包；
2、模块之间可以单向依赖，如同jar包之间一样；
3、模块可以被替换；
4、模块以功能划分，模块内部再按层次或技术划分；
5、移除一个模块尽量不影响其他模块的功能，比如移除日志功能，其他模块应该能够做最小改动；
总之，把模块当做jar包，把jar包当成模块。模块内部的组织，参考jar包内部的组织。

